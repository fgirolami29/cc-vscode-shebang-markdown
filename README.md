# cc-vscode-shebang-markdown

VS Code extension that detects a custom shebang and switches files to Markdown for linters.

A tiny VS Code extension that checks the first line of a document and forces Markdown language mode when it matches a CodeCorn shebang.
